box: matsengrp/cpp

build:
    steps:
        - script:
            name: build ham
            code: scons

deploy:
    steps:
        - github-upload-asset:
            token: $GITHUB_TOKEN
            file: hample
            release_id: 1853187
